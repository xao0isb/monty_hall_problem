# frozen_string_literal: true

RSpec.describe MontyHallProblem do
  it "has a version number" do
    expect(MontyHallProblem::VERSION).not_to be_nil
  end

  # it "does something useful" do
  #   expect(false).to eq(true)
  # end
end
